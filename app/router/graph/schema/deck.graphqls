type Deck {
  id: ID!
  userId: String!
  name: String!
  createdAt: Timestamp!
  updatedAt: Timestamp!
}

extend type Query {
  decks: [Deck!]!
  deck(id: ID!): Deck!
}

input CreateDeckInput {
  name: String!
}

extend type Mutation {
  createDeck(input: CreateDeckInput!): Deck!
}
